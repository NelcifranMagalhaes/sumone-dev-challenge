<%= notice %>
&nbsp;
<div class="container">
    <%= form_tag('/drinks') do %>
        <%= hidden_field_tag 'search','simple' %>
        <div class="form-group">
          <%= text_field_tag 'name',nil ,class: 'form-control',placeholder: "Try a Martini..."%>
        </div>
        <div class="form-group">
          <%= submit_tag "Search", class: 'btn btn-outline-primary btn-lg' %>
        </div>
    <% end %>
</div>
<div class="container">
    <%= form_tag('/drinks') do |f| %>
        <%= hidden_field_tag 'search','advanced' %>
        <div class="form-group">
          <%= select_tag 'temperature',options_for_select({ 'hot': 0, 'warm': 1,'ambient': 2, 'cold': 3,'extra cold': 4}),class: 'form-control',prompt: "Best temperature.."%>
        </div>
        <div class="form-group">
          <%= select_tag 'alcohol_level',options_for_select(['Yes','No' ]),class: 'form-control',prompt: "Want to be drunked?.."%>
        </div>
        <div class="form-group">
          <%= select_tag 'sugar',options_for_select(['Yes','No' ]),class: 'form-control',prompt: 'Sugar?..'%>
        </div>
        <div class="form-group">
          <%= submit_tag "Search", class: 'btn btn-outline-primary btn-lg' %>
        </div>
    <% end %>
</div>

  <div class="text-center">
    <%= paginate @drinks%>
  </div>

<div class="container">
  <div class="row">
    <% @drinks.each do |drink| %>
      <section>
        <div class="container" align="center">
          <div class="row align-items-center">
            <div class="col-lg-6 order-lg-2">
              <div class="p-5">
                <%= image_tag drink.image_url, class: 'img-fluid img-thumbnail drink-card__img' %>
              </div>
            </div>
            <div class="col-lg-6 order-lg-1">
              <div class="p-5">
                <h2 class="display-4"><%= drink.name %></h2>
                <p><%= drink.description %></p>
              </div>
            </div>
          </div>
        </div>
      </section>
    <% end %>
  </div>
</div>
  <div class="text-center">
    <%= paginate @drinks%>
  </div>


